[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: slower tests using real threads/timing/disk I/O instead of mocks - still no Docker needed for this service (unlike reporting-output's integration tests). Run with -m integration to select just these, -m "not integration" to skip them.
